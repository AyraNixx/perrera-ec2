$(document).ready(function(){
    $('#insert').on('show.bs.modal', function(event) {});
    $('select["name=especies_id"]').on('click', generate_species);
})

function generate_area(){}

function generate_species(){
    
}
